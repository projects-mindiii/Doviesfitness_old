<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>

            <gradient
                    android:startColor="@color/colorBlack"
                    android:centerColor="@color/colorBlack"
                    android:centerY="0.75"
                    android:endColor="@color/colorBlack"
                    android:angle="270"
                    />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>

                <gradient
                        android:startColor="@color/splash_screen_color"
                        android:centerColor="@color/splash_screen_color"
                        android:centerY="0.75"
                        android:endColor="@color/splash_screen_color"
                        android:angle="270"
                        />
            </shape>
        </clip>
    </item>
    <item
            android:id="@android:id/progress"
            >
        <clip>
            <shape>

                <gradient
                        android:startColor="#BCD488"
                        android:endColor="#BCD488"
                        android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>